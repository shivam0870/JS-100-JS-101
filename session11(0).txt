Javascript includes() function 

JavaScript includes() function checks if string contains a specified element or substring,
returning true if found and false otherwise.

console.log("There are 10 apples that are kept in the basket".includes("apple")); //true

Javascript substring() function 

The JavaScript substring() method is used to extract parts of a string. It takes two indices as parameters, specifying the start and end points of the substring to be returned.

